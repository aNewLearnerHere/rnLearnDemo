import {
    EventEmitter
} from 'fbemitter';

export default class EmitterTimeout extends EventEmitter {
    emit(event, ...args) {
        setTimeout(() => super.emit(event, ...args), 0);
    }
    on(...args) {
        this.addListener(...args);
    }
}
