import _ from 'lodash';

export default {
    debounce: function (func, wait, options) {
        return _.debounce(func, wait || 300, options || { leading: true, trailing: false });
    },
    throttle: _.throttle,
};