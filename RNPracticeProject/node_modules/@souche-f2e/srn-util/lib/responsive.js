import { Dimensions } from 'react-native';

const responsive = function(baseWidth) {
    return function(num) {
        return parseInt(num * Dimensions.get('window').width / (baseWidth || 750))
    }
};

export default responsive