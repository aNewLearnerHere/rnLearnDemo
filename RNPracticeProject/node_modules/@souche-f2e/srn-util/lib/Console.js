/**
 * sentry 不再此处使用，由于该代码多个包中使用，所以先发一个版本:0.2.69，
 * 此版本置空保留原来所有方法，避免其他使用出报错
 */

export default class Console {
	constructor(options) {
	}

	// 设置故障用户信息
	setUserContext(user) {
	}

	// 设置 tags 信息，可用于日志后台分组、过滤
	setTagsContext(tags) {
	}

	// 设置 extra 信息
	setExtraContext(extra) {
	}

	// 清除用户信息
	clearUserContext() {
	}

	error(exception, extra) {
	}

	log() {}

	debug() {}

	dir() {}

	info() {
		// 看情况决定是否记录
		//      Raven.captureMessage('', {
		//          level: 'info',      // 设置日志级别
		//      });
	}

	table() {}

	warn() {
		// 看情况决定是否记录
		//      Raven.captureMessage('', {
		//          level: 'waning',    // 设置日志级别
		//      });
	}
}