const packageJSON = require('../../package.json');

export default {
    appName: 'framework',
    appVersion: packageJSON.version,
    sentry: {
        version: packageJSON.version,
        dsn: ''
    }
}