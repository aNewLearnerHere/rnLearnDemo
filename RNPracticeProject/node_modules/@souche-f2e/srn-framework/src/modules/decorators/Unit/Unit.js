
function Unit(type){
    return function (target, name, descriptor){
        let v = (descriptor.initializer && descriptor.initializer.call(this))||0;
        // 记录原始单位
        if(!target.constructor.__unitOriginals__){
            Object.defineProperty(target.constructor,"__unitOriginals__",{"value":{

            },enumerable:false,writeable:true,configurable:true});
        }
        target.constructor.__unitOriginals__[name] = {
            type:type
        };
        return descriptor
    }

}

export default Unit