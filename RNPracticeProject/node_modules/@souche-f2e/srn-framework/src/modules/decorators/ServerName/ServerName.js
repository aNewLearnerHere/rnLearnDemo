function Alias (aliasName) {
    return function (target, name, descriptor) {
        let v = descriptor.initializer && descriptor.initializer.call(this);
        if (!target.constructor.__aliasNames__) {
            Object.defineProperty(target.constructor, "__aliasNames__", {
                "value": {}, enumerable: false, writeable: true, configurable: true
            });
        }
        target.constructor.__aliasNames__[name] = aliasName;
        return descriptor;
    }
}

export default Alias;